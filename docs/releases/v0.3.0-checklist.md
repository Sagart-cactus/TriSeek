# TriSeek v0.3.0 Release Checklist

Use this as the release gate for the Memo launch.

## Release Metadata

- [x] Bump workspace version to `0.3.0`
- [x] Add `docs/releases/v0.3.0.md`
- [x] Update install snippets and user-facing version examples to `v0.3.0`
- [x] Update README and MCP docs for Memo tools, install modes, and search cache behavior

## Automated Regression

- [x] `cargo fmt --all --check`
- [x] `cargo clippy --workspace --all-targets --locked -- -D warnings`
- [x] `cargo test --workspace --locked`
- [x] Search-cache unit coverage for TTL/LRU/invalidation
- [x] MCP integration coverage for cache hit/miss/bypass and reindex invalidation
- [x] Memo daemon coverage for:
  - unknown/fresh/stale transitions
  - cross-session stale propagation
  - redundant-read accounting
  - `current_tokens` stale hints
  - `memo_check` recommendation thresholds
- [x] Memo MCP forwarding coverage for:
  - `memo_check`
  - `memo_status`
  - `_meta.session_id` / `_meta.sessionId`
- [x] Installer template coverage for OpenCode and Pi generated plugin/extension files

## Smoke Validation

- [x] `target/release/triseek doctor`
- [x] Temp-workspace installer smoke for Claude project scope
- [x] Temp-home installer smoke for Codex config + hooks
- [x] OpenCode plugin generation smoke
- [x] Pi extension generation smoke
- [ ] Run the checklist in `docs/memo-installer-verification.md`
- [x] Run `scripts/release_smoke.sh` against release binaries

## Release Packaging

- [ ] Push branch updates and open/update PR
- [ ] Merge PR into the release branch or `main`
- [ ] Push tag `v0.3.0`
- [ ] Confirm GitHub Actions publishes release artifacts

## Known Limits To Keep In Notes

- [x] Codex Memo is active-mode only for now
- [x] Pi template still whitelists `bash`, but the shim does not classify Bash as a Memo event
