# TriSeek v0.3.1 Release Checklist

Use this as the release gate for the post-`v0.3.0` patch release.

## Release Metadata

- [x] Bump workspace version to `0.3.1`
- [x] Add `docs/releases/v0.3.1.md`
- [x] Update install snippets and user-facing version examples to `v0.3.1`
- [x] Capture merged branches since `v0.3.0` in the release notes

## Automated Regression

- [x] `cargo fmt --all --check`
- [x] `cargo clippy --workspace --all-targets --locked -- -D warnings`
- [x] `cargo test --workspace --locked`
- [x] Hidden-path regression coverage stays green:
  - `cargo test -p search-index --test integration`
  - `cargo test -p triseek rg_searches_allowlisted_hidden_paths_by_default`
- [x] Installer default-scope coverage stays green:
  - `cargo test -p triseek`

## Smoke Validation

- [x] `target/release/triseek doctor`
- [x] Run `scripts/release_smoke.sh` against release binaries

## Release Packaging

- [ ] Push branch updates and open/update PR
- [ ] Merge PR into the release branch or `main`
- [ ] Push tag `v0.3.1`
- [ ] Confirm GitHub Actions publishes release artifacts

## Known Limits To Keep In Notes

- [x] Default search still excludes noisy hidden paths unless `--include-hidden` is set
- [x] Codex Memo is active-mode only for now
